# ISP IP 中转服务 — 需求文档

| 项 | 内容 |
|---|---|
| 文档版本 | v1.1 |
| 日期 | 2026-06-06 |
| 状态 | 草稿 |
| 变更 | v1.1 新增「安全防御需求」(§7)，针对美国 VPS 公网暴露场景 |

---

## 1. 背景与目标

通过自建一台中转 VPS，把客户的代理流量转发到从 decode 购买的家宽 ISP 静态出口（SOCKS5 代理），为每个客户提供独立出口线路，并自动生成 vmess/vless/clash 订阅链接。

核心诉求：**当有新客户需要时，能从 decode 即时购买一条线路，快速配置进系统并生成订阅链接** —— 即「下单即开通」的流水线能力。

**部署环境**：中转 VPS 部署于**美国机房**，面板/订阅/SSH 均暴露于公网，因此安全防御为硬性要求（详见 §7）。

**用途声明**：仅用于本人及授权客户的合规代理用途。

---

## 2. 架构设计

```
客户A ──vmess/vless──┐
客户B ──vmess/vless──┤──> 中转VPS (Marzban + Xray) ──┬─SOCKS5→ decode线路A
客户C ──vmess/vless──┘                                ├─SOCKS5→ decode线路B
                                                      └─SOCKS5→ decode线路C
       ↑ 一人一条订阅              ↑ 一人一条路由规则          ↑ 一人一个 outbound
```

| 组件 | 形态 | 说明 |
|---|---|---|
| **客户端** | vmess / vless / clash | 通过订阅链接接入 |
| **中转 VPS** | 自购 1 台 VPS，运行 Marzban + Xray | 接收客户流量、按用户分流、生成订阅 |
| **落地线路** | decode 提供的 SOCKS5 代理串 `ip:port:user:pass` | 纯代理端点，**非 VPS，无法安装软件** |

**分流机制**：所有客户共用一个 inbound（vless/vmess + ws + tls，走 443），Xray 根据用户名（email 字段）将每个客户的流量路由到其专属的 SOCKS5 outbound。

---

## 3. 技术选型

| 角色 | 选型 | 理由 |
|---|---|---|
| 面板 | **Marzban** | 提供完整 REST API，可脚本化「加 outbound + 路由 + 用户 + 出订阅」全流程；订阅自动聚合 clash/v2ray |
| 核心 | **Xray**（Marzban 内置） | 多协议、SOCKS5 出站、路由分流 |
| 出口转发 | **Xray outbound (SOCKS5)** | 直接将 decode 线路配为出站，无需额外转发工具 |

> 备选：3X-UI（手动操作更直观，但每次开通需手点面板，自动化弱）。因本项目强调「快速开通」，选 Marzban。
> 已排除：Marzban-node —— 落地是 SOCKS5 端点而非 VPS，无法部署节点。

---

## 4. 功能需求

### FR-1 中转面板部署
- 在自购 VPS 上部署 Marzban + Xray
- 创建 sudo 管理员账号
- 建立一个共享 inbound（vless/vmess + ws + tls）供所有客户接入

### FR-2 线路开通（核心）
通过开通脚本 `add_line.py`，输入「客户名 + decode SOCKS5 线路串」后自动完成：
1. 向 Xray core 配置追加一个 SOCKS5 outbound（tag = `out-<客户名>`）
2. 追加路由规则：`user=<客户名>` → `out-<客户名>`
3. 创建 Marzban 用户
4. 输出该用户订阅链接（clash/v2ray 通用）

调用示例：
```bash
python3 add_line.py zhang 1.2.3.4:8080:user:pass --days 30
```

### FR-3 线路更换（幂等）
- 同一客户名重复执行脚本 = 更新其出口线路
- 客户的订阅链接保持不变，无需客户重新导入

### FR-4 订阅生成
- 每个客户拥有独立订阅链接
- 支持 clash 与 v2ray/vmess 格式

### FR-5 用户管理
- 支持设置有效期（`--days`，0 = 不限）
- 支持设置流量上限（`--gb`，0 = 不限）
- 到期 / 超流量自动断开

---

## 5. 开通操作流程（运营视角）

每来一个客户：
1. 从 decode 购买一条家宽 ISP 线路 → 得到 `ip:port:user:pass`
2. 执行一条命令 `python3 add_line.py <客户名> <线路串> --days 30`
3. 将输出的订阅链接发给客户

---

## 6. 非功能需求

| 项 | 要求 |
|---|---|
| 开通耗时 | 单客户 < 1 分钟（一条命令） |
| 可用性 | 中转 VPS 单点；后续可考虑多 VPS 冗余 |
| 安全 | 面板 HTTPS；管理员强密码；脚本配置文件中的凭据妥善保管 |
| 隔离 | 各客户流量按用户名分流，互不可见、互不影响 |
| 安全 | 见 §7 安全防御需求（硬性） |
| 合规 | 仅服务本人及授权客户 |

---

## 7. 安全防御需求（美国 VPS 公网暴露）

中转 VPS 在公网上承受三类威胁：① 主机被入侵（SSH 爆破、面板提权）；② 代理被主动探测/扫描而封锁；③ 客户滥用导致 abuse 投诉或被当跳板攻击内网。需求按这三类划分。

### SEC-1 主机与 SSH 加固
- SSH 仅密钥登录，禁用密码登录与 root 直登，改非默认端口
- 安装 fail2ban，自动封禁爆破 IP
- 启用系统自动安全更新
- 以非 root sudo 用户运维

### SEC-2 防火墙最小暴露
- 默认拒绝所有入站，仅放行：SSH 端口、443（代理 + 伪装）
- **面板端口不对公网开放**

### SEC-3 面板隐藏与保护
- Marzban Dashboard 不直接暴露公网：绑定 `127.0.0.1`，通过 SSH 隧道访问；或置于反代之后 + 随机路径 + IP 白名单
- 管理员强密码，修改默认 Dashboard 路径
- 全程 HTTPS

### SEC-4 反主动探测 / 抗封锁
- 使用**真实域名 + 有效 TLS 证书**，443 端口对外表现为正常 HTTPS 站点
- 配置**伪装回落**：直连 IP 或探测无效请求时回落到一个真实网站，不暴露代理特征
- 拒绝以 IP 直连（SNI 不匹配域名即断开）
- 评估采用 **VLESS-Reality** 作为更强的抗探测入站方案

### SEC-5 出站滥用防护（防跳板 + 降低投诉）
- **禁止客户流量访问私网/保留地址**（`10/8`、`172.16/12`、`192.168/16`、`127/8`、`169.254/16`、本机 IP），防止客户横向攻击面板、内网或云元数据 `169.254.169.254`
- **封禁出站 25 端口（SMTP）**，防垃圾邮件投诉
- 按需封禁 BitTorrent，降低美国机房 DMCA/abuse 投诉风险
- 每客户流量/有效期限制（见 FR-5）

### SEC-6 订阅链接安全
- 订阅 token 长随机、仅 HTTPS 下发
- 链接泄露可一键吊销/轮换（不影响其他客户）

### SEC-7 凭据与日志
- decode 线路账号、面板管理员密码以环境变量或 600 权限文件保存，不入版本库、不入明文日志
- Xray 日志级别 warning，不记录用户访问内容（隐私 + 减少留存）
- 面板登录异常告警

### SEC-8 抗 DDoS / 限速
- 启用 VPS 厂商 DDoS 防护；面板/订阅入口可经 Cloudflare 代理
- 订阅与登录接口做速率限制（nginx limit_req + fail2ban）

---

## 8. 待确认事项（部署后验证）

| # | 事项 | 影响 |
|---|---|---|
| 1 | **Xray 中用户 email 的实际格式**：可能是纯 `用户名`，也可能是 `用户名@inbound`。需通过 `marzban logs \| grep -i email` 确认，并据此校正路由规则 `user` 字段的匹配值。 | 决定 FR-2 分流是否生效，**必须验证** |
| 2 | 共享 inbound 的具体参数（端口、ws path、TLS 证书/域名） | 影响客户端接入 |
| 3 | 是否需要网页版开通界面（Flask 表单封装脚本） | 二期可选 |
| 4 | 是否需要流量/到期告警通知 | 二期可选 |

---

## 9. 交付物

- [x] 开通脚本 `add_line.py`（已完成初版）
- [ ] Marzban 部署（VPS 上执行）
- [ ] 共享 inbound 配置
- [ ] 路由 email 格式实测与脚本校正
- [ ] **主机加固**（SSH/fail2ban/防火墙，SEC-1/2）
- [ ] **面板隐藏**（绑 localhost + SSH 隧道，SEC-3）
- [ ] **反探测伪装回落 / Reality**（SEC-4）
- [ ] **出站滥用规则**（封私网 + 25 端口 + BT，SEC-5）
- [ ] （可选）网页开通界面
- [ ] （可选）流量/到期告警

---

## 10. 后续可扩展方向

- 网页化开通面板（点击即开通）
- 客户自助续费 / 计费（对接 Xboard / V2board 类计费面板）
- 多中转 VPS 负载与故障切换
- 线路健康检查（自动探测 decode 线路可用性，失效告警）
