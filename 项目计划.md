# ISP IP 中转服务 — 项目计划

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 日期 | 2026-06-06 |
| 关联文档 | [需求文档.md](需求文档.md) · [技术设计与研发方案.md](技术设计与研发方案.md) |
| 计划起始 | 2026-06-09（周一） |
| 估算口径 | 单人投入，工作日数为净工时；日历按每周 5 个工作日推算 |

> 目标节奏：**第 1 周末能手动开通第一个真实客户（MVP）**，第 3 周末有可用的 Web 开通面板，第 4 周末完成安全验收。二期（健康告警 / 规模化）视运营情况启动。

---

## 1. 阶段与里程碑总览

| 阶段 | 名称 | 里程碑 | 退出标准 | 估算 |
|---|---|---|---|---|
| **P0** | 采购与准备 | M0 资源就绪 | VPS / 域名 / decode 试用线路到位 | 0.5d |
| **P1** | 加固 + 面板上线（MVP） | **M1 首单打通** | 手动 + CLI 开通第一个客户，订阅可用 | 2d |
| **P2** | RelayHub 服务化 | M2 服务层 | 脚本拆为模块 + 单测；护栏纳入 | 3d |
| **P3** | Web 开通面板 | M3 面板可用 | 原型接真 API，CRUD + 吊销订阅 | 4d |
| **P4** | 安全收尾与验收 | **M4 验收通过** | 反探测/出站护栏/面板隐藏全部验证 | 2d |
| **P5** | 运营增强（二期） | M5 健康告警 | 线路探活 + 到期/流量 Telegram 告警 | 3d |
| **P6** | 规模化（二期） | M6 多节点 | 多 VPS / 分布式锁 / 计费对接 | 待定 |

一期（P0–P4）合计约 **11.5 个工作日**。

---

## 2. 时间线（建议日历）

```
周次   日期(2026)        阶段        关键产出
W1     06-09 ~ 06-13   P0 + P1     M0 资源就绪 → M1 首单打通(MVP 上线)
W2     06-16 ~ 06-20   P2          RelayHub 服务化 + 单测
W3     06-23 ~ 06-27   P3          Web 面板接真 API, CRUD 完成
W4     06-30 ~ 07-04   P4          安全验收通过 → 一期交付
W5+    07-07 起         P5/P6       二期(健康告警/规模化), 按需启动
```

> 里程碑 **M1（首单打通）= 06-13 前**、**M4（验收通过）= 07-04 前** 为两个硬节点。

---

## 3. 任务分解（WBS）

### P0 采购与准备（M0）
| # | 任务 | 依赖 | 退出标准 |
|---|---|---|---|
| 0.1 | 购置美国 VPS（建议带 DDoS 防护机房） | — | 可 SSH 登录 |
| 0.2 | 注册/解析域名到 VPS | 0.1 | A 记录生效 |
| 0.3 | decode 购买 1 条试用 SOCKS5 线路 | — | 拿到 `ip:port:user:pass` |

### P1 加固 + 集成栈上线（M1，对应 [deploy/README.md](deploy/README.md)）
> Marzban 已集成进项目，由 docker compose 一键拉起（不再手动逐个安装）。
> inbound 默认 VLESS-Reality（无需域名/证书）；安全护栏由容器 bootstrap 自动置顶。

| # | 任务 | 依赖 | 退出标准 | SEC |
|---|---|---|---|---|
| 1.1 | 主机加固：SSH 密钥/禁密码/非默认端口 | 0.1 | 仅密钥可登录 | SEC-1 |
| 1.2 | 防火墙 + fail2ban + 自动更新（仅放行 443/SSH） | 1.1 | 8000/8080 不对外 | SEC-1/2 |
| 1.3 | 装 Docker + Compose | 0.1 | `docker compose version` 正常 | — |
| 1.4 | 配 `.env` + `setup-reality.sh` 生成密钥 | 1.3 | 生成 xray_config.json | SEC-4 |
| 1.5 | `docker compose up -d` 拉起整栈 | 1.4 | bootstrap 日志「护栏已就位」 | SEC-3/5 |
| 1.6 | **实测 email 格式**并校正路由匹配 | 1.5 | 分流命中正确出口 | — |
| 1.7 | 开通首个客户（Web 或 CLI） | 1.5,1.6 | **订阅可用，走 decode 出口** | — |

> ⚠️ 任务 1.6 是全项目最高优先级风险点（见 §5），务必在 1.7 前实测。
> 0.2 域名非必需（Reality 不需要证书）；如改用 ws+tls 才需要域名。

### P2 RelayHub 服务化（M2，对应技术文档 B3/B4）
| # | 任务 | 依赖 | 退出标准 |
|---|---|---|---|
| 2.1 | 搭工程骨架（目录/依赖/.env.example） | M1 | `pip install` 通过 |
| 2.2 | 抽 `config / parsing / models` | 2.1 | 单测通过 |
| 2.3 | 实现 `guard`（护栏置顶纯函数）+ 单测 | 2.2 | **断言护栏恒置顶** |
| 2.4 | 实现 `marzban` 客户端 | 2.2 | respx mock 通过 |
| 2.5 | 实现 `provisioning`（编排+锁）+ 集成测试 | 2.3,2.4 | provision 幂等通过 |
| 2.6 | `add_line.py` 改薄封装复用服务 | 2.5 | CLI 行为与 M1 等价 |

### P3 Web 开通面板（M3，对应技术文档 B5）
| # | 任务 | 依赖 | 退出标准 |
|---|---|---|---|
| 3.1 | FastAPI 骨架 + `/api/lines` POST（开通） | M2 | 表单提交出订阅 |
| 3.2 | `/api/lines` GET + `/api/stats`（总览/统计） | 3.1 | 表格/卡片有真实数据 |
| 3.3 | 删除客户 + 吊销订阅接口 | 3.1 | CRUD 完整 + SEC-6 |
| 3.4 | 原型 `prototype.html` 接真 API | 3.1,3.2 | 点击即开通/总览 |
| 3.5 | systemd 部署，绑 127.0.0.1 | 3.4 | 开机自启 + 隧道访问 |

### P4 安全收尾与验收（M4，对应技术文档 C3）
| # | 任务 | 依赖 | 退出标准 |
|---|---|---|---|
| 4.1 | 安全回归测试纳入 CI（护栏不被旁路） | M2 | 测试常态通过 |
| 4.2 | 反探测验证：IP 直连 443 返回伪装站 | 1.4 | 探测无代理特征 |
| 4.3 | 出站验证：客户无法访问内网/元数据/25 | 1.5 | 全部被拦 |
| 4.4 | 凭据/日志核查（env、无明文） | M2 | 符合 SEC-7 |
| 4.5 | 走查验收清单，签收一期 | 4.1-4.4 | 验收标准全绿 |

---

## 4. 依赖关系（关键路径）

```
0.1 VPS ─► 1.1 加固 ─► 1.3 面板 ─┬─► 1.4 inbound ─► 1.6 email实测 ─► 1.7 首单(M1)
0.2 域名 ───────────────────────┘                                        │
0.3 线路 ───────────────────────────────────────────────────────────────┘
   M1 ─► P2 服务化(M2) ─► P3 Web(M3) ─► P4 验收(M4)
```
**关键路径**：`VPS → 加固 → 面板 → inbound → email实测 → 首单 → 服务化 → Web → 验收`。
P4 的 4.1 可与 P3 并行（依赖 M2 即可）。

---

## 5. 风险登记（含计划应对）

| 风险 | 概率 | 影响 | 应对 | 关联 |
|---|---|---|---|---|
| **email 格式不符导致分流失败** | 中 | 高 | 任务 1.6 前置实测，预留 0.5d 缓冲 | 技术文档 A6 |
| 域名被墙/被探测 | 中 | 中 | 回落伪装 + 评估 Reality（1.4/二期） | SEC-4 |
| 护栏回归失效 | 低 | 高 | 2.3 纯函数单测 + 4.1 CI 回归 | SEC-5 |
| decode 线路不稳 | 中 | 中 | 二期健康检查（P5）；当前人工巡检 | — |
| 单 VPS 故障 | 低 | 高 | 二期多节点（P6） | — |
| abuse/DMCA 投诉 | 中 | 中 | 封 25 端口 + 可选封 BT；遵守机房 ToS | SEC-5 |

---

## 6. 一期交付物清单（Definition of Done）

- [ ] 加固后的美国 VPS（SSH/防火墙/fail2ban）
- [ ] Marzban + Xray，共享 inbound + 回落伪装
- [ ] 出站安全护栏（私网/本机/25 置顶）
- [ ] RelayHub 服务（config/parsing/guard/marzban/provisioning）+ 单测
- [ ] CLI `add_line.py`（薄封装）
- [ ] Web 开通面板（开通/总览/删除/吊销订阅）
- [ ] 安全回归测试 + 验收清单全绿
- [ ] 文档：需求 / 技术设计与研发方案 / 项目计划（本套）

---

## 7. 二期候选（P5/P6，按运营触发）

| 触发条件 | 启动项 |
|---|---|
| 客户数 > 10 或线路常掉线 | P5 线路健康检查 + 到期/流量 Telegram 告警 |
| 需对外售卖 / 自助续费 | 计费面板（Xboard/V2board）对接 |
| 单机扛不住 / 需多地出口 | P6 多 VPS + 分布式锁 + 主控下发 |
