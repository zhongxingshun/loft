# ISP IP 中转服务 — 技术设计与研发方案

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 日期 | 2026-06-06 |
| 关联文档 | [需求文档.md](需求文档.md) · [项目计划.md](项目计划.md) |
| 状态 | 草稿 |
| 说明 | 由原《设计文档 v1.1》与《研发设计 v1.0》合并，并修正合并前不一致项 |

> 本文档分三部分：**A 系统设计**（架构/数据流/配置/安全）· **B 研发方案**（工程结构/模块/接口/测试）· **C 落地**（部署/验收/扩展）。里程碑与排期见独立的 [项目计划.md](项目计划.md)。

---

# A. 系统设计

## A1. 概述

系统以一台美国 VPS 为中转核心，运行 **Marzban 面板 + Xray 内核**，将客户的代理流量按用户名分流到各自专属的 decode SOCKS5 出口，并通过 **RelayHub** 服务（CLI + Web）实现「一键开通 → 出订阅」。对应需求 FR-1~5、SEC-1~8。

## A2. 系统架构

### A2.1 部署拓扑

```
                          ┌─────────────────────────────────────────┐
                          │            中转 VPS (单机, 美国)         │
                          │                                          │
  客户端 ──443/wss──────► │  Nginx/Xray ─► Marzban ─► Xray Core      │
  (clash/v2ray)           │     │                        │           │
                          │     │  REST API              │ outbound  │
  运营 ──RelayHub───────► │  ───┘  /api/*                │ (socks5)  │
  (CLI/Web, 仅本地)       │                              │           │
                          └──────────────────────────────┼───────────┘
                                                         │
                          ┌──────────────┬───────────────┼──────────────┐
                          ▼              ▼               ▼              ▼
                     decode线路A    decode线路B     decode线路C    ...(按客户增长)
                     (家宽SOCKS5)   (家宽SOCKS5)    (家宽SOCKS5)
```

### A2.2 组件职责

| 组件 | 职责 |
|---|---|
| **Marzban 面板** | 用户/订阅/流量管理；提供 REST API；持有 Xray core 配置 |
| **Xray Core** | 入站接收、按路由规则分流、SOCKS5 出站 |
| **共享 Inbound** | 单个 vless+ws+tls 入站，承载所有客户接入 |
| **per-客户 Outbound** | 每客户一个 SOCKS5 出站，指向其 decode 线路 |
| **路由表 (routing.rules)** | 安全护栏置顶 + 按用户名映射到对应 outbound |
| **RelayHub** | 开通编排服务，CLI 与 Web 共用；封装 Marzban API + 安全护栏 |

## A3. 核心数据流

### A3.1 客户流量路径

```
客户端 ─ vless over ws/tls (域名:443) ─►
[Inbound: VLESS_WS_INBOUND]  ── Xray 解析 user.email = "<客户名>"
  ─► [Routing] 安全护栏(置顶,放行非拦截目标) ─► 匹配 user=="<客户名>" → out-<客户名>
  ─► [Outbound socks5] → decode线路 ─► 目标网站(以 decode 家宽 IP 出口)
```

### A3.2 开通数据流

```
开通(CLI 或 Web) →
  1. POST /api/admin/token        → access_token
  2. GET  /api/core/config        → 当前 Xray 配置
       └ 内存: 确保 block 出站 → upsert outbound → 重排规则(护栏置顶+本客户分流)
  3. PUT  /api/core/config        → 写回并热重载 Xray
  4. POST /api/user (409→PUT)     → 创建/更新用户
  5. 读 subscription_url          → 返回完整订阅链接
```

## A4. 配置模型

### A4.1 共享 Inbound（部署时手工建一次）

```jsonc
{
  "tag": "VLESS_WS_INBOUND", "listen": "0.0.0.0", "port": 443,
  "protocol": "vless",
  "settings": { "clients": [], "decryption": "none" },   // clients 由 Marzban 动态注入
  "streamSettings": {
    "network": "ws", "security": "tls",
    "wsSettings": { "path": "/<随机路径>" },              // 非默认 /ray (SEC-4)
    "tlsSettings": { "certificates": [ /* 域名证书 */ ] }
  }
}
```

### A4.2 per-客户 Outbound（RelayHub 生成）

```jsonc
{
  "tag": "out-zhang", "protocol": "socks",
  "settings": { "servers": [{
    "address": "1.2.3.4", "port": 8080,
    "users": [{ "user": "decodeUser", "pass": "decodePass" }]   // 无鉴权则省略
  }]}}
```

### A4.3 路由表顺序（关键不变量）

```jsonc
// routing.rules —— 顺序恒定：
[
  /* ① 安全护栏(置顶, SEC-5) */
  { "type":"field", "outboundTag":"block", "ip":["geoip:private","<本机IP>/32"] },
  { "type":"field", "outboundTag":"block", "port":"25" },
  /* ② 各客户分流 */
  { "type":"field", "user":["zhang"], "outboundTag":"out-zhang" },
  ...
]
```
> 需 outbounds 含 `{ "tag":"block","protocol":"blackhole" }`。护栏置顶由 RelayHub 每次写配置时重建保证（见 B4.3）。

### A4.4 命名规约

| 对象 | 规则 | 示例 |
|---|---|---|
| Marzban 用户名 | 客户名（唯一，`^[a-zA-Z0-9_]{2,32}$`） | `zhang` |
| Outbound tag | `out-<客户名>` | `out-zhang` |
| 路由匹配 user | `<客户名>`（实际 email 格式待实测，见 A6） | `zhang` |

一个客户 = {1 用户, 1 outbound, 1 路由规则}，通过客户名强绑定，便于幂等更新与删除。

## A5. 安全设计（SEC-1 ~ SEC-8）

### A5.1 威胁模型与防御分层

```
            公网 (美国 VPS 暴露面)
 ①主机入侵        ②代理被探测/封锁       ③客户滥用/跳板
     │                  │                     │
 ┌───▼────┐     ┌───────▼───────┐     ┌───────▼────────┐
 │主机加固 │     │  入站伪装层    │     │  出站管控层     │
 │SSH/防火 │     │TLS/回落/Reality│     │私网封锁/25/BT  │
 │fail2ban│     │面板隐藏        │     │每用户限额       │
 └────────┘     └───────────────┘     └────────────────┘
```

### A5.2 端口暴露矩阵（SEC-2）

防火墙默认 `deny incoming / allow outgoing`，仅放行：

| 端口 | 用途 | 暴露范围 | 说明 |
|---|---|---|---|
| `2222`(示例) | SSH | 公网（建议 IP 白名单） | 非默认端口 + fail2ban |
| `443` | 代理入站 + 网站伪装 | 公网 | 唯一对外业务端口 |
| `8000` | Marzban Dashboard | **仅 127.0.0.1** | SSH 隧道访问 |
| `8080` | RelayHub Web | **仅 127.0.0.1** | SSH 隧道访问 |
| 出站 `25` | SMTP | **封禁** | 防垃圾邮件投诉 |

### A5.3 主机加固层（SEC-1）

| 项 | 配置 |
|---|---|
| SSH | 密钥登录、禁密码、禁 root 直登、非默认端口 |
| fail2ban | 监控 sshd 与面板登录，自动封爆破 IP |
| 自动更新 | `unattended-upgrades` 自动安全补丁 |
| 账户 | 非 root sudo 用户运维 |

### A5.4 入站伪装层 —— 反主动探测（SEC-3/4）

| 机制 | 设计 |
|---|---|
| 真实域名 + 有效证书 | Let's Encrypt，443 对外即正常 HTTPS |
| 伪装回落 | 探测/非法请求回落真实网页（Nginx 反代或 Xray `fallbacks`） |
| 拒绝 IP 直连 | SNI 不匹配域名即断开 |
| 强化方案 | 评估升级 **VLESS-Reality**，借真实大站指纹，抗探测更强 |
| 面板隐藏 | Dashboard/Web 绑 `127.0.0.1`，`ssh -L` 隧道访问 |

### A5.5 出站管控层 —— 防跳板与滥用（SEC-5）

客户流量经 VPS 出站，必须前置 block 护栏（见 A4.3），拦截：
- 私网/保留地址 `geoip:private`（含云元数据 `169.254.169.254`）
- 本机公网 IP（防客户回打面板/SSH）
- 出站 25 端口（防垃圾邮件）；可选 BitTorrent（降低 DMCA 投诉）

**实现保证**：RelayHub 每次写配置时重建护栏并置顶（见 B4.3），CLI/Web 均不可旁路。

### A5.6 订阅 / 凭据 / 抗 DDoS（SEC-6/7/8）

| 维度 | 措施 |
|---|---|
| 订阅(SEC-6) | 长随机 token，仅 HTTPS；泄露可 `revoke_sub` 重置，仅影响该客户 |
| 凭据(SEC-7) | 面板/decode 凭据来自 env 或 600 文件，不入库、不入明文日志 |
| 日志(SEC-7) | Xray `loglevel: warning`，不记录访问内容 |
| 抗 DDoS(SEC-8) | VPS 厂商防护；订阅/登录接口 Nginx `limit_req` + fail2ban |
| 隔离 | 各客户独立 outbound，流量互不可见；出口为 decode 家宽 IP |

## A6. 关键风险与验证项

| 风险 | 说明 | 验证/对策 | 影响 |
|---|---|---|---|
| **email 格式不确定** | 路由 `user` 字段需匹配实际 email，版本间可能为 `name` 或 `name@inbound` | `marzban logs \| grep -i email` 实测后校正 | **高** |
| block 护栏被旁路 | 若护栏未置顶则 SEC-5 失守 | **已解决**：RelayHub 每次重建护栏置顶 + 安全回归测试（B7） | 已缓解 |
| 面板公网暴露 | Dashboard/Web 开公网易被爆破 | 仅监听 127.0.0.1 + SSH 隧道 | 高 |
| 单点故障 | VPS 宕机全员中断 | 二期多 VPS / 健康检查 | 中 |
| decode 线路失效 | 家宽 IP 掉线 | 二期线路探活告警 | 中 |
| core 配置并发覆盖 | 高并发开通竞态 | RelayHub 进程锁串行化 | 低 |

---

# B. 研发方案（RelayHub）

## B1. 范围与目标

把现有「单文件 [add_line.py](add_line.py) + 静态 [prototype.html](prototype.html)」工程化为可维护服务 **RelayHub**：服务层（编排+护栏）被 CLI 与 Web 共用；Web 接原型 UI 实现开通与总览。非目标（二期）：计费、多节点、自动探活告警。

## B2. 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 语言 | Python 3.11+ | 与现有脚本一致 |
| Web | FastAPI + Uvicorn | 异步、自带 OpenAPI |
| 校验 | pydantic v2 / pydantic-settings | 模型与配置校验、密钥经 env |
| HTTP | httpx | 同步/异步统一 |
| 前端 | 原生 HTML/JS（原型演进） | 零构建 |
| 测试 | pytest + respx | 无需真面板即可集成测试 |
| 运行 | systemd | 开机自启、日志托管 |

## B3. 代码结构

```
relayhub/
├── app/
│   ├── config.py          # Settings: 面板地址/凭据/inbound/护栏开关 (env)
│   ├── parsing.py         # parse_socks(): 线路串解析
│   ├── guard.py           # 出站护栏: block_rules() + reorder_rules() (SEC-5)
│   ├── marzban.py         # MarzbanClient: 鉴权 / core config / user CRUD
│   ├── provisioning.py    # ProvisioningService: 开通/换线/删除/吊销 编排
│   ├── models.py          # LineSpec / ProvisionResult / CustomerView
│   └── api.py             # FastAPI 路由 (承载原型 UI)
├── web/index.html         # 由 prototype.html 演进, 接真实 API
├── scripts/add_line.py    # CLI 薄封装, 复用 ProvisioningService
├── tests/                 # test_parsing / test_guard / test_provisioning
├── .env.example
├── requirements.txt
└── systemd/relayhub.service
```

**依赖方向**：`api / scripts → provisioning → {marzban, guard, parsing, models}`。`provisioning` 是唯一编排者，CLI 与 Web 都经它 → 行为一致、安全护栏不可旁路。

## B4. 模块详细设计

### B4.1 config.py
```python
class Settings(BaseSettings):
    marzban_url: str; admin_user: str; admin_pass: str   # 凭据来自 env (SEC-7)
    shared_inbound_tag: str = "VLESS_WS_INBOUND"
    shared_inbound_protocol: str = "vless"
    verify_tls: bool = True
    local_ip: str = ""; block_smtp: bool = True; block_bittorrent: bool = False
    model_config = SettingsConfigDict(env_file=".env", env_prefix="RELAYHUB_")
```

### B4.2 parsing.py
`parse_socks(line) -> SocksEndpoint`：`ip:port` / `ip:port:user:pass`，非法抛 `ValueError`。

### B4.3 guard.py — 出站安全护栏（核心不变量）
```python
def block_rules(local_ip, block_smtp, block_bt) -> list[dict]: ...
def ensure_blackhole_outbound(outbounds: list[dict]) -> None: ...
def reorder_rules(rules, customer_rule, out_tag, guard) -> list[dict]:
    """返回 [guard(置顶)] + [customer_rule] + [其余非block非本客户规则]"""
```
**不变量**：每次写配置后路由表首段恒为 guard。`reorder_rules` 为纯函数，由单测锁定顺序正确性（修复合并前的「block 被旁路」高危项）。

### B4.4 marzban.py — API 客户端（仅 HTTP+鉴权）
```python
class MarzbanClient:
    def _token() -> str             # POST /api/admin/token, 缓存+过期重取
    def get_core_config() -> dict
    def put_core_config(cfg)        # 热重载
    def upsert_user(body) -> dict   # POST, 409 -> PUT /api/user/{name}
    def list_users() -> list[dict]
    def delete_user(name)
    def revoke_sub(name) -> dict    # SEC-6
```

### B4.5 provisioning.py — 开通编排（核心）
```python
class ProvisioningService:
    _lock = threading.Lock()        # core config 读改写串行化
    def provision(spec: LineSpec) -> ProvisionResult   # 开通/换线 (幂等)
    def remove(name)                # 删 user + out-<name> + 其路由规则
    def rotate_sub(name) -> str     # 吊销并重置订阅 token (SEC-6)
    def list_customers() -> list[CustomerView]
```
`provision` 流程（持锁）：
```
with lock:
    cfg = client.get_core_config()
    guard.ensure_blackhole_outbound(cfg["outbounds"])
    upsert_outbound(cfg, out_tag, sock)                 # 幂等
    cfg["routing"]["rules"] = guard.reorder_rules(...)  # 护栏置顶 + 本客户分流
    client.put_core_config(cfg)                         # 失败即止, 不建用户
user = client.upsert_user(build_user_body(spec))
return ProvisionResult(sub_url=abs_url(user.subscription_url), ...)
```

### B4.6 models.py
```python
class LineSpec(BaseModel):       name:str(pattern); line:str; days:int=30; gb:float=0
class ProvisionResult(BaseModel):name; sub_url; exit; out_tag; expire_days
class CustomerView(BaseModel):   name; exit; status:Literal["ok","warn","bad"]; used_gb; limit_gb; expire
```

## B5. Web 接口（FastAPI，承载原型 UI）

| 方法 | 路径 | 入参 | 返回 | 对应原型 |
|---|---|---|---|---|
| POST | `/api/lines` | `LineSpec` | `ProvisionResult` | 「开通新线路」表单 |
| GET | `/api/lines` | — | `list[CustomerView]` | 总览表格 |
| GET | `/api/stats` | — | 统计 dict | 顶部 4 卡 |
| POST | `/api/lines/{name}/rotate-sub` | — | `{sub_url}` | 吊销/重置订阅 |
| DELETE | `/api/lines/{name}` | — | `204` | 删除客户 |
| GET | `/` | — | `index.html` | 面板首页 |

约定：统一错误体 `{error:{code,message}}`；`ValueError`→400，Marzban 异常→502。面板仅监听 `127.0.0.1`（SEC-3），一期不做公网鉴权。

## B6. CLI 复用

`scripts/add_line.py` 重构为薄封装，命令行不变：
```python
spec = LineSpec(name=args.name, line=args.line, days=args.days, gb=args.gb)
print(ProvisioningService(MarzbanClient(settings), settings).provision(spec).sub_url)
```
→ CLI 与 Web 共用同一编排与护栏。

## B7. 并发 / 错误 / 日志 / 测试

| 主题 | 方案 |
|---|---|
| 并发一致性 | `ProvisioningService` 进程级 `Lock`；写配置失败不建用户（用户步骤可安全重跑） |
| 分层异常 | `ParsingError(400)` / `MarzbanError(502)` / `ConfigWriteError(500)`，api 层统一转 HTTP |
| 日志 | 结构化 json line，记 `action/name/out_tag/结果`；**不记**凭据明文与访问内容(SEC-7) |
| 测试 | 单元：parse_socks、block_rules、**reorder_rules 断言护栏恒置顶**、provision 幂等；集成：respx mock Marzban 跑全流程；**安全回归**：既有规则下 provision 后护栏仍置顶 |

---

# C. 落地

## C1. 部署步骤

> **部署模型：容器化集成栈（推荐）。** Marzban + Xray + RelayHub 由 `docker compose` 统一编排，
> 一键拉起，详见 [deploy/README.md](deploy/README.md)。入站默认 **VLESS-Reality**（无需域名/证书）。
> 流程：主机加固 → `cp .env.example .env` 填值 → `bash deploy/setup-reality.sh` 生成密钥 →
> `docker compose up -d`。容器内 `scripts/bootstrap.py` 自动确保安全护栏置顶。
>
> 下方为**非容器/手动**部署（备选，ws+tls + 系统级 Marzban），仅在不使用 Docker 时参考。

```bash
# ===== 阶段一: 主机加固 (先做, SEC-1/2) =====
# 1. 非 root sudo 用户 + SSH 公钥; sshd_config: PasswordAuthentication no / PermitRootLogin no / Port 2222
sudo systemctl restart ssh
# 2. 防火墙: 默认拒绝, 仅放行 SSH + 443
sudo ufw default deny incoming && sudo ufw default allow outgoing
sudo ufw allow 2222/tcp && sudo ufw allow 443/tcp && sudo ufw enable
# 3. fail2ban + 自动更新
sudo apt install -y fail2ban unattended-upgrades

# ===== 阶段二: 安装面板 =====
# 4. 安装 Marzban
sudo bash -c "$(curl -sL https://github.com/Gozargah/Marzban-scripts/raw/master/marzban.sh)" @ install
# 5. 创建管理员(强密码)
marzban cli admin create --sudo
# 6. 面板绑本地: .env UVICORN_HOST=127.0.0.1 (SEC-3); 访问 ssh -L 8000:127.0.0.1:8000 user@vps -p 2222

# ===== 阶段三: 业务配置 =====
# 7. 域名证书 + Settings>Core 建共享 inbound(vless+ws+tls, A4.1) + 回落伪装
# 8. routing 顶部加 block 护栏: 私网+本机IP+25端口 (A4.3)  ← RelayHub 也会自动维护
# 9. 配置 RelayHub .env (面板地址/账号/inbound/local_ip) (SEC-7)
# 10. 实测 email 格式并校正路由匹配 (A6)

# ===== 阶段四: 验证 =====
# 11. 开通首个客户
python3 scripts/add_line.py test 1.2.3.4:8080:u:p --days 1
# 12. 安全验证: 客户连上后无法访问 127.0.0.1/内网/169.254.169.254/出站25; 直连IP访问443返回伪装站
```

systemd（RelayHub Web）：
```ini
[Service]
EnvironmentFile=/opt/relayhub/.env        # 600 权限 (SEC-7)
ExecStart=/opt/relayhub/.venv/bin/uvicorn app.api:app --host 127.0.0.1 --port 8080
Restart=always
```

## C2. 二期扩展（预留）

| 方向 | 设计要点 |
|---|---|
| 线路健康检查 | 定时探测每条 outbound，失败告警；CustomerView.status 转真实探测 |
| 流量/到期告警 | 轮询 `/api/users` 用量，近阈值 Telegram 通知 |
| 多中转冗余 | 多 VPS 跑 Xray，主控下发；分布式锁替换进程锁 |
| 计费自助 | 接入 Xboard/V2board，客户自助下单续费 |

## C3. 验收标准（对齐需求）

- [ ] CLI 与 Web 开通同一客户结果一致（FR-2/3）
- [ ] 开通后路由表首段恒为护栏，客户无法访问 `127.0.0.1`/内网/`169.254.169.254`/出站25（SEC-5，测试+实连）
- [ ] 面板/Web 仅监听 127.0.0.1，公网无法直连（SEC-3）
- [ ] 订阅可一键吊销重置，互不影响其他客户（SEC-6）
- [ ] 凭据仅来自 env、日志无明文（SEC-7）

## C4. 附：组件交互时序

```
开通入口        RelayHub           Marzban API         Xray Core
 │ provision ─►│ POST /admin/token ►│                   │
 │             │ GET /core/config  ►│                   │
 │             │ (护栏置顶 + outbound + 本客户规则)       │
 │             │ PUT /core/config  ►│ ── reload ──────► │ 热加载
 │             │ POST /user        ►│                   │
 │ ◄ 订阅链接 ─│ ◄ subscription_url │                   │
```
